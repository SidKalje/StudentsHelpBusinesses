import { createContext } from "react";

export const isLoggedIn = createContext({}); // [1]
export const UserUID = createContext({}); // [1]
export const UserEmail = createContext({}); // [1]
export const UserDisplayName = createContext({}); // [1]
export const isUserStudent = createContext({}); // [1]
